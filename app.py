import streamlit as st
st.set_page_config(page_title="Тест по География", page_icon="🌍")
st.title(" Тест по География")
st.write("Отговорете на всички въпроси и натиснете „Провери резултата“.")
questions = [
    ("Коя е столицата на България?", "София"),
    ("Кой континент е България?", "Европа"),
    ("Коя река минава през Русе?", "Дунав"),
    ("Кое е най-високото планинско било в България?", "Рила"),
    ("Кое море мие източното крайбрежие на България?", "Черно море")
]
answers = []
for i, (question, _) in enumerate(questions):
    answer = st.text_input(f"{i+1}. {question}")
    answers.append(answer)
if st.button("Провери резултата"):
    score = 0
    for user_answer, (_, correct_answer) in zip(answers, questions):
        if user_answer.strip().lower() == correct_answer.lower():
            score += 1
    st.write(f"###  Резултат: {score} / 5")
    if score == 5:
        st.success(" Отлично! Всички отговори са верни!")
    elif score >= 3:
        st.info(" Добър резултат!")
    else:
        st.error(" Трябва още малко упражнения.")
